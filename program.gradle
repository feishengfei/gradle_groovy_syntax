/* static import */
import static Boolean.FALSE

/* static import alias */
import static Calendar.getInstance as now

/* static start import */
import static java.lang.Math.*


/* import aliasing */

task t << {
	assert !FALSE
	assert now().class == Calendar.getInstance().class
	println 'now().class = ' + now().class

	println 'sin(0) = ' + sin(0)
	println 'sin(PI/2) = ' + sin(java.lang.Math.PI / 2.0)
	println 'sin(PI/6) = ' + sin(java.lang.Math.PI / 6.0)
	println 'cos(0) = ' + cos(0)
	println 'cos(PI/2) = ' + cos(java.lang.Math.PI / 2.0)
	println 'cos(PI/3) = ' + cos(java.lang.Math.PI / 3.0)
}

public class MultiplyTwo {
	def static multiply(def value) {
		return value * 3 //intentionally wrong.
	}
}
